<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jamesatkin.monies">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.MainActivity"
            android:label="Dashboard">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.AddPurchaseActivity"
            android:label="Add Purchase"
            android:parentActivityName=".activities.MainActivity" />
        <activity
            android:name=".activities.ViewPurchasesActivity"
            android:label="View Purchases"
            android:parentActivityName=".activities.MainActivity" />
        <activity
            android:name=".activities.EditPurchaseActivity"
            android:label="Edit Purchase"
            android:parentActivityName=".activities.ViewPurchasesActivity" />
        <activity
            android:name=".activities.SettingsActivity"
            android:label="Settings"
            android:parentActivityName=".activities.MainActivity" />
        <activity
            android:name=".activities.AddTypeActivity"
            android:label="Add Type"
            android:parentActivityName=".activities.ManageTypesActivity" />
        <activity
            android:name=".activities.ManageTypesActivity"
            android:label="Manage Types"
            android:parentActivityName=".activities.SettingsActivity" />
        <activity
            android:name=".activities.EditTypeActivity"
            android:label="Edit Type"
            android:parentActivityName=".activities.ManageTypesActivity" />
        <activity android:name=".activities.TypeActivity" />
        <activity android:name=".activities.trialActivity" />
        <activity
            android:name=".activities.StatisticsActivity"
            android:label="Statistics"
            android:parentActivityName=".activities.MainActivity" />
        <activity
            android:name=".activities.SelectTypeIconActivity"
            android:label="Select Icon"></activity>
    </application>

</manifest>